Bootstrap: docker
From: r-base:4.3.1

%post

  # apt
  apt update
  apt install -y libssl-dev libxml2-dev libcurl4-openssl-dev libicu-dev libicu60

  # mlr3
  R --slave -e 'install.packages("stringi", type = "source")'
  R --slave -e 'install.packages("batchtools")'

%runscript
  Rscript $@

