Bootstrap: docker
From: r-base:4.3.1

%post

  # apt
  apt update
  apt install libssl-dev -y
  apt install libxml2-dev -y
  apt install libcurl4-openssl-dev -y
  apt install libicu-devel -y

  # mlr3
  R --slave -e 'install.packages("batchtools")'

%runscript
  Rscript $@

